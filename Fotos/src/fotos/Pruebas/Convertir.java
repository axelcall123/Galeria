/*package fotos;
CLASE DE CODIGO MUERTO

import java.awt.image.BufferedImage;
import java.io.File;

import java.io.IOException;
import javax.imageio.ImageIO;

public class Convertir {
  public static boolean convertFormat(String entrada,String salida, String extension) throws IOException {
        BufferedImage input_image = null;
        input_image = ImageIO.read(new File(entrada)); //LEE EL JPG
        File outputfile = new File(salida); //CREA EL BMP
        boolean result=ImageIO.write(input_image, extension, outputfile); //REGRESA FALSO O VERDADERO SI EJECUTO
        return result;
    }  
}*/ 
